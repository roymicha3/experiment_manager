#####################################################################################
#   Experiment Configuration for Custom Metrics Test                              #
#####################################################################################

name: "custom_metrics_test"
desc: "Test experiment for custom batch metrics with multiple key-value pairs"
settings: # the base configuration of the experiment

trials: # each trial changes the parameters it chooses - this is later merged with the trials.yaml
