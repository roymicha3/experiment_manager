name: "batch_gradient_env"
workspace: null # Will be set by test

trackers:
  - type: "LogTracker"
    verbose: false
  - type: "DBTracker"
    name: "batch_gradient_experiment.db"
    recreate: true
