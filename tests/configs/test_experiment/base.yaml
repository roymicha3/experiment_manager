#####################################################################################
#   Base Configuration - contains all the objects that are used in the experiment   #
#####################################################################################


# Pipeline
pipeline:
  type: "DummyPipeline"
  epochs: 3
  batch_size: 64
  validation_split: 0.2
  test_split: 0.2
  shuffle: True

  # Callbacks (Optional)
  callbacks: